# 应用层代码审查问题清单

> 审查范围：`app/src/main/java/com/tyranor/next`（89 个 Kotlin 文件，约 17,700 行）
> 审查依据：`AGENT.md` 审核规范（代码逻辑 / 架构 / 健壮性 / 回归风险 / 代码规范五个独立角度）
> 验证方式：4 个独立审查 Agent 并行审查 + 2 个独立验证 Agent 逐条读码复核，**31 条全部确认属实，0 误报**
> 审查日期：2026-09-01

## 问题统计

| 严重级别  | 数量 | 说明                                                         |
| ----- | -- | ---------------------------------------------------------- |
| Major | 8  | 功能缺陷、架构错层、明文规范违反（M6 经确认豁免、M7/M8 已修复后不再计入待修复）               |
| Minor | 20 | 边界竞态、重复代码、规范一致性偏差                                          |
| 豁免    | 1  | M6 快捷启动卡游戏名字号，已写入 AGENT.md 明文豁免条款                          |
| 已修复   | 2  | M7 弹窗背景 PageGrey → NavWhite；M8 弹窗改走 Compose AppAlertDialog |

| 类别        | 数量 |
| --------- | -- |
| 代码逻辑      | 5  |
| 架构 / 回归风险 | 7  |
| 代码健壮性     | 5  |
| 代码规范      | 14 |

***

## Major 问题

### M1. VNDB 抓取的 vndbId/metadataTitle 全链路被丢弃，数据库元数据列永远为空

- [ ] 待修复

- **类别**：代码逻辑

- **位置**：[CoverScraperService.kt:360-364](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/core/cover/CoverScraperService.kt#L360-L364)

- **描述**：`VndbCoverService.fetchBestCover`（VndbCoverService.kt:43-48）特意返回带 `vndbId` / `metadataTitle` 的结果，但整条生效链路都在丢弃这两个字段：

  1. 批量刮削：`scrapeLibraryCovers` 经 `asCoverOnlyUpdateFrom`（CoverScraperService.kt:360-364）转换，该函数仅复制 `coverUri` / `coverSource` 两字段；
  2. 合并层 `mergeScrapedCover`（CoverScrapeTaskManager.kt:104-112）同样只比较/复制封面两字段；
  3. 手动绑定：`bindCoverCandidate`（CoverScraperService.kt:113-133）返回值不带 `vndbId`；唯一写入 `vndbId` 的 `VndbCoverService.bindCandidate`（VndbCoverService.kt:51-65）在全项目零调用方。
     与之矛盾的是 `EngineScanner.rescanLibrary`（EngineScanner.kt:411-412）特意保护这两列不被扫描清空，说明它们是有意维护的数据，但实际永远为空。

  > 验证备注：`updateGameCover`（EngineScanner.kt:176-191）实际比较并落库四列，问题根源是上游 transform 输入已无新值；grep 确认全项目唯一新值来源就是被剥离的 `VndbCoverService` 产物。

- **修复建议**：

  1. `asCoverOnlyUpdateFrom` 增加复制源返回值的 `vndbId` / `metadataTitle`（非空时覆盖）；
  2. `mergeScrapedCover` 同步比较并合并这两个字段；
  3. `bindCoverCandidate` 对 VNDB 候选填充 `vndbId` / `metadataTitle`（可委托给现存的 `VndbCoverService.bindCandidate`）。

### M2. core 层反向依赖 theme 包，形成 core↔theme 循环并间接引入 Compose 类型

- [ ] 待修复

- **类别**：架构

- **位置**：[EngineLauncher.kt:38](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/core/game/launch/EngineLauncher.kt#L38)、[EngineLauncher.kt:336-343](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/core/game/launch/EngineLauncher.kt#L336-L343)

- **描述**：`EngineLauncher` 第 38 行 `import com.tyranor.next.theme.AppThemeColors`，第 336-343 行在 `buildIntent` 中读取 `AppThemeColors.isDark` / `primaryArgb` 注入引擎 Intent。而 `theme/AppThemeColors.kt` 第 9 行已 import `core.settings.AppSettingsStore`（theme→core），core→theme 使两包形成循环。且 `AppThemeColors` 基于 `androidx.compose.runtime.mutableStateOf` 与 `androidx.compose.ui.graphics.Color`，使 core 间接依赖 Compose 类型，违反 AGENT.md「core 不得依赖 Compose UI 组件」及固定依赖方向（界面层 → 功能抽象层 → 引擎层）。

- **修复建议**：在 core 内定义纯数据载荷类（如 `ThemeColorPayload`：darkMode / primaryArgb / card / text / muted），由 ui 层调用点或 Application 启动时从 `AppThemeColors` 填充注入 core 可读的快照提供者；或将 `buildIntent` 尾部的主题色注入逻辑上移到 ui 层包装。

### M3. core 层存在 @Composable 组合函数，Compose UI 逻辑错置于功能抽象层

- [ ] 待修复

- **类别**：架构

- **位置**：[AppLocaleController.kt:50-66](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/core/i18n/AppLocaleController.kt#L50-L66)

- **描述**：第 9-13 行 import `androidx.compose.runtime.*` 与 `androidx.compose.ui.platform.*`；第 50-66 行定义 `@Composable` 函数 `ProvideAppLocale`（CompositionLocalProvider 提供 LocalContext / LocalConfiguration），被 8 个 Activity 使用（MainActivity、SaveManagementActivity、CoverScraperSettingsActivity、KrkrOnlinePatchActivity、PerGameSettingsActivity、AppSettingsActivity、EngineSettingsMenuActivity、EngineSettingsActivity）。违反 AGENT.md「core 不得依赖 Compose UI 组件」。同文件第 20-47 行的 wrap / localeOf / findActivity 纯逻辑属 core 合理职责。

- **修复建议**：将 `ProvideAppLocale` 迁移到 `ui/common`（如 `ui/common/AppLocale.kt`），`core/i18n` 仅保留非组合逻辑；ui 层 import 路径同步更新（8 处）。

### M4. krkr 补丁安装任务与瞬态 UI 状态绑定，旋转后可并发重复安装并产生临时文件写入竞态

- [ ] 待修复

- **类别**：代码健壮性

- **位置**：[KrkrOnlinePatchActivity.kt:118-235](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/patch/KrkrOnlinePatchActivity.kt#L118-L235)

- **描述**：

  1. `installing` / `entries` / `message`（:120-123）均为 `remember`（非 `rememberSaveable`），下载协程运行在 `rememberCoroutineScope()`（:118，:220 scope.launch）中；
  2. 旋转后旧协程作用域被取消，但 `KrkrOnlinePatchService.downloadAndInstall` 内是 HttpURLConnection + copyTo 等无挂起点的阻塞 IO，下载不中断，会继续写完游戏目录；
  3. 重建后的 UI 中 `installing` 复位为 false，安装按钮重新可用，用户再次点击启动第二个并发任务；
  4. 两个任务在 `downloadAndInstall`（KrkrOnlinePatchService.kt:72）使用同一固定路径临时文件 `File(downloadDir, fileName)`，各自以 truncate 模式交错写入；先完成者的 `finally` 中 `tempFile.delete()`（:78-80）会删除另一任务正在写的临时文件，最终 `copyIntoGameDir` 可能把损坏的补丁 xp3 复制进游戏目录；
  5. 旋转还会丢失已加载的补丁索引列表与勾选状态，`LaunchedEffect(Unit)` 触发重复网络请求。

- **修复建议**：将下载安装任务与状态上收到 ViewModel（配合 Job 句柄与 installing StateFlow）；或在服务层为 `downloadAndInstall` 增加进程级互斥（参照 `GameSaveManager.importLock`）并保证临时文件唯一（`File.createTempFile`）；UI 状态改用 `rememberSaveable` 持久化关键信息。

### M5. 快捷启动卡引擎名使用禁用的 headlineSmall 字号

- [ ] 待修复

- **类别**：代码规范（文字尺寸两档制）

- **位置**：[HomeScreen.kt:408-413](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/home/HomeScreen.kt#L408-L413)

- **描述**：快捷启动卡片引擎名 `Text` 使用 `MaterialTheme.typography.headlineSmall`。AGENT.md「页面内容文字尺寸规范」明文禁止 `bodySmall / bodyLarge / labelMedium / labelLarge / headlineMedium / headlineSmall` 等其它排版尺寸，页面内容只允许 `titleMedium` / `bodyMedium` 两档。

- **修复建议**：改为 `MaterialTheme.typography.titleMedium`（可加 `FontWeight.Bold` 保持视觉层级）。

### M6. 快捷启动卡游戏名使用 titleLarge，超出两档制

- [x] 已豁免：经确认属有意设计，已作为明文豁免条款写入 AGENT.md「页面内容文字尺寸规范」（快捷启动卡游戏名 `titleLarge` Bold 不受两档制限制），无需修改代码

- **类别**：代码规范（文字尺寸两档制）

- **位置**：[HomeScreen.kt:414-422](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/home/HomeScreen.kt#L414-L422)

- **描述**：卡片内游戏名 `Text` 使用 `typography.titleLarge`。原判定与 AGENT.md「titleLarge 仅顶部栏标题可用」冲突；后经确认该卡片为封面模糊图背景 + 白字展示的特殊场景，属有意设计。

- **处理结果**：在 AGENT.md 文字尺寸规范中新增明文豁免条款（QuickLaunchCard 游戏名 `titleLarge` Bold），本条不再作为问题跟踪。

### M7. AppAlertDialog 弹窗背景使用 PageGrey 而非白色

- [x] 已修复：`containerColor` 已由 `PageGrey` 改为 `NavWhite`（浅色默认=白，且随「色调切换」实时计算），KDoc 注释与实现已一致

- **类别**：代码规范（弹窗规范）

- **位置**：[AppAlertDialog.kt:105](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/common/AppAlertDialog.kt#L105)

- **描述**：Card 容器 `containerColor = PageGrey`。`PageGrey` 浅色默认取 `GreyLight(0xFFF2F3F5)`（灰）而非纯白，toneSwitch 开启时才为白，深色模式为近黑。AGENT.md 明文「所有弹窗背景必须为白色」，且组件容器规范指明弹窗内容项应引用 `NavWhite` 而非 `PageGrey`。第 48 行 KDoc 注释自称「背景白色」与实现矛盾。

- **修复建议**：`containerColor` 改为 `NavWhite`（浅色默认=白，且随「色调切换」实时计算，同时符合组件容器色规范）。

### M8. Artemis 补丁确认弹窗使用 View 体系 AlertDialog，未统一走 AppAlertDialog

- [x] 已修复：原生 `Context.confirmArtemisPatchChoice` 已删除，新增 Compose 挂起版 `awaitArtemisPatchChoice`（`setContent` + `ProvideAppLocale` + `AppAlertDialog`，复用同一份按钮→策略映射）；GameShortcutActivity 蹦床已改走该 Compose 弹窗

- **类别**：代码规范（弹窗规范）

- **位置**：[ArtemisPatchChoiceDialog.kt:80-104](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/game/ArtemisPatchChoiceDialog.kt#L80-L104)、[GameShortcutActivity.kt:56-60](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/game/GameShortcutActivity.kt#L56-L60)

- **描述**：第 3 行 `import android.app.AlertDialog`，`confirmArtemisPatchChoice` 用 `AlertDialog.Builder` 弹出系统样式弹窗。违反 AGENT.md「所有弹窗统一用 AppAlertDialog」「所有弹窗背景必须为白色，且圆角必须使用统一圆角数值 8dp」——白底 / 8dp 圆角 / 统一排版均不可控。使用场景 GameShortcutActivity 为纯 ComponentActivity 透明蹦床（无 setContent），但技术上完全可以 `setContent` + Compose 弹窗（其余 Activity 均如此）。

- **修复建议**：蹦床改为 `setContent` + `AppAlertDialog` 的 Compose 版（复用本文件的按钮→策略映射）；或在 AGENT.md 中为该场景明确豁免。

### M9. 统一顶部栏入口 PlaceholderPage 全项目无调用点（死代码）且自身不符规范

- [ ] 待修复

- **类别**：代码规范（顶部栏规范）

- **位置**：[PlaceholderPage.kt:23-56](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/common/PlaceholderPage.kt#L23-L56)

- **描述**：AGENT.md 称顶部栏「当前已由统一入口 PlaceholderPage 实现，新页面应复用」，但全项目 grep 仅有定义、AGENT.md 与 docs 引用，**零实际调用点**。且组件自身违反规范：第 38 行标题未加 `FontWeight.Bold`（规范要求），顶部栏容器缺少 `Modifier.background(colorScheme.background)` 不透明背景层（规范第 6 节结构图的第一层）。此问题是 M10 十处手写复制的根因。

- **修复建议**：按规范补齐（加粗标题、补 background 层）后替换各页面手写顶部栏；若决定放弃该组件则删除，并修订 AGENT.md 表述。

### M10. 顶部栏结构在 10 处页面各自复制手写，未复用统一组件

- [ ] 待修复

- **类别**：代码规范（顶部栏规范 + 重复代码）

- **位置**（10 处，共同骨架：Column + background + statusBarsPadding + 64dp + horizontal 16dp + titleLarge Bold）：

  1. [HomeScreen.kt:131-141](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/home/HomeScreen.kt#L131-L141)
  2. [EngineScreen.kt:75-87](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/engine/EngineScreen.kt#L75-L87)
  3. [GameScreen.kt:397-447](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/game/GameScreen.kt#L397-L447)
  4. [SaveManagementActivity.kt:159-177](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/save/SaveManagementActivity.kt#L159-L177)
  5. [KrkrOnlinePatchActivity.kt:147-168](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/patch/KrkrOnlinePatchActivity.kt#L147-L168)
  6. [SettingsScreen.kt:545-567](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/settings/SettingsScreen.kt#L545-L567)
  7. [SettingsScreen.kt:628-639](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/settings/SettingsScreen.kt#L628-L639)（SettingsTopBar）
  8. [AppSettingsActivity.kt:114-130](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/settings/AppSettingsActivity.kt#L114-L130)
  9. [EngineSettingsMenuActivity.kt:104-123](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/settings/EngineSettingsMenuActivity.kt#L104-L123)
  10. [PerGameSettingsScreen.kt:235-250](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/settings/PerGameSettingsScreen.kt#L235-L250)、[CoverScraperSettingsActivity.kt:330-347](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/cover/CoverScraperSettingsActivity.kt#L330-L347)

- **描述**：违反 AGENT.md「所有页面共用 PlaceholderPage（或与之一致的结构），禁止各页面各自实现不同样式的顶部栏」。验证确认取色来源已三方漂移：Material 页用 `colorScheme.background/onBackground`，Miuix 页用 `MiuixTheme.colorScheme.background/onBackground`，CoverScraperTopBar 用 `PageGrey/TextColor`——印证「各自实现、统一入口失效」。

- **修复建议**：在 `ui/common` 提供统一 TopBar / MiuixTopBar 组件（支持可选右侧图标槽），Material 与 Miuix 页面共用，单点维护 64dp / 加粗 / 背景色等规范细节。

### M11. GameActionRow 手写 Row 拼装条目，跳转条目未复用 AppNavItem

- [ ] 待修复

- **类别**：代码规范（功能跳转条目规范 + 重复代码）

- **位置**：[GameScreen.kt:1322-1360](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/game/GameScreen.kt#L1322-L1360)

- **描述**：`GameActionRow`（Row + clip 8dp + NavWhite + 横向 16dp + 左图标 24dp + bodyMedium）与 `AppNavItem`（AppNavItem.kt:61-109）实现逐要素对应，差异仅在无右箭头、纵向 padding、图标主题色 tint、多 danger 态。调用点 GameScreen.kt:698-716 中「存档管理」(:701)、「在线补丁」(:709)、「引擎设置」(:715) 是明确的「进入下一级」跳转条目，落入 AGENT.md 强制使用 AppNavItem 的范畴（且位于弹窗 GameActionsSheet 内，规范明确覆盖弹窗内条目）。

- **修复建议**：跳转类条目改用 `AppNavItem`；如需保留动作语义差异，扩展 AppNavItem 增加 danger / 隐藏箭头参数，消除两套平行实现。

***

## Minor 问题

### m1. SAF 兜底建目录硬编码校验目录名 "savedata"，ONS 的 "save" 目录成功也返回 false

- [ ] 待修复

- **类别**：代码逻辑

- **位置**：[EngineLauncher.kt:532-563](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/core/game/launch/EngineLauncher.kt#L532-L563)

- **描述**：函数末行 :560 `current.name.equals("savedata", ignoreCase = true)` 将成功条件绑定到目录名。ONS 启动分支（:283-291）在非 scoped 时传入以 `"save"` 结尾的路径，即使逐级 createDirectory 全部成功（ok=true），名称比对失败仍返回 false，`saveDirReady` 仅为 false 并打 warning，存档目录不保证存在。仅 krkr 调用点（:526/:528 传 savedata）恰好匹配。当前因启动前已强制「所有文件访问」权限，mkdirs 失败场景罕见。

- **修复建议**：去掉对目标目录名的硬编码校验，改为记录解析出的 relative 目标并在逐级创建完成后验证「已到达 relative 末端」（如比较创建段数或最终 canonical 路径与目标一致）。

### m2. saveQuickLaunch 无锁写入共享缓存，addQuickLaunch 读-判-写非原子，存在丢失更新竞态

- [ ] 待修复

- **类别**：代码逻辑

- **位置**：[EngineScanner.kt:264-299](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/core/game/scan/EngineScanner.kt#L264-L299)

- **描述**：`addQuickLaunch`（:268-274）执行 `loadQuickLaunch`（无锁 volatile 读）→ 判重/判容量 → `saveQuickLaunch`（:292-294 直接 `quickLaunchCache = snapshot`），全程不持 `cacheLock`。同文件所有其他缓存写路径均在 `synchronized(cacheLock)` 内（updateGames:149、updateGameCover:172、recordRecentGame:197、removeRecentGame:219、updateRecentGames:242、saveRoot:312、removeRoot:325），唯独快捷启动读写例外。两线程并发 `addQuickLaunch` 时后保存者覆盖前者（游戏丢失）。

  > 验证备注：「超 3 槽」子句实际难以发生（单次 add 写入长度=快照+1≤3），核心问题是丢失更新。当前主要调用方实际串行，风险被掩盖，但 EngineScanner 是公开 object，契约上并发调用即破坏不变量。

- **修复建议**：将 `addQuickLaunch` / `removeQuickLaunch` / `saveQuickLaunch` 纳入 `synchronized(cacheLock)`，读-判-写合并为加锁的单次操作。

### m3. cleanTitle 清理正则缺少词边界，标题含 patch/complete/trial 子串时被误截断

- [ ] 待修复

- **类别**：代码逻辑

- **位置**：[CoverSupport.kt:109-123](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/core/cover/CoverSupport.kt#L109-L123)

- **描述**：:119 正则 `"(?i)complete|$localizedEditionWords|trial|patch"` 无 `\b` 词边界，replace 替换标题中任意位置的子串。可证实反例："Dispatch" 中 "patch" 命中 → 清洗结果 "Dis "。清洗结果直接作为 Hikarinagi(:176,:199) / Bangumi(:250,:262) / Steam(:305,:323) / VNDB(VndbCoverService.kt:92) 的搜索关键字，轻则搜不到正确条目，重则匹配到错误游戏返回错误封面。

- **修复建议**：为 alternation 加词边界，如 `"(?i)\\b(complete|trial|patch)\\b|$localizedEditionWords"`（中文词无需边界），或分词后按完整词匹配再重组。

### m4. 令牌迁移在 Keystore 解密瞬时失败时清空 legacy 明文令牌，登录态不可逆丢失

- [ ] 待修复

- **类别**：代码逻辑 + 健壮性

- **位置**：[HikarinagiAuthStore.kt:61-73](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/core/auth/HikarinagiAuthStore.kt#L61-L73)

- **描述**：`ensureMigrated` 的 hasSecureTokens 分支（:65-67）比较 legacy 明文与 `SecureAuthStore.decryptField` 结果，:68-69 不一致即 `legacy.edit().clear()` 并置 `FIELD_MIGRATED=true`，永不再重试。但 `SecureAuthStore.decryptField`（SecureAuthStore.kt:45-62）在 Keystore 瞬时异常（注释明确举例「冷启动早期」）时 `runCatching` 吞掉异常返回空串且不抛出，外层 :97 catch 兜不住。此时 legacy 非空、secure 为空串，被误判为「迁移失败后用户已重新登录」，明文令牌被清空；若密文最终不可解（如换机恢复备份后 Keystore 密钥不匹配），登录态彻底丢失。

- **修复建议**：清除 legacy 前先确认 secureAccess 解密成功（校验密文非空且 decryptField 未走失败路径）；解密失败时保留 legacy 明文并 return（下次重试迁移），只有确认「密文可解且令牌确实不同」时才清空。

### m5. core 设置/存储/模型层持有 androidx.compose.runtime 可观察状态（UI 状态泄漏到 core）

- [ ] 待修复

- **类别**：架构（边界性违规）

- **位置**：

  - [AppSettingsStore.kt:5-6](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/core/settings/AppSettingsStore.kt#L5-L6)（languageState:55、navStyleState:85、gameSortState:88、coverScraperSettingsVersion:91）

  - [HikarinagiAuthStore.kt:5-6](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/core/auth/HikarinagiAuthStore.kt#L5-L6)（:32 statusVersion）

  - [CoverScrapeTaskManager.kt:6-7](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/core/cover/CoverScrapeTaskManager.kt#L6-L7)（:30-31 State\<CoverScrapeTaskState>）

  - [ScanGame.kt:5](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/core/game/model/ScanGame.kt#L5)（@Immutable）

- **描述**：core 下共 12 处 `androidx.compose.*` import。`compose.runtime` 严格说非「UI 组件」，属边界性违规，但将 UI 重组状态沉淀在功能抽象层会阻碍 core 独立演化。

- **修复建议**：新增代码不再扩散该模式；如需严格分层，可将内存态改为 `kotlinx.coroutines.flow.StateFlow` 由 ui 层 collectAsState，或上移到 ui 层 ViewModel；至少在 AGENT.md 中明确「core 允许 compose.runtime 状态」的边界约定。

### m6. theme→ui/common 反向 import，theme 与 ui 形成包循环

- [ ] 待修复

- **类别**：架构

- **位置**：[MiuixSettingsTheme.kt:9](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/theme/MiuixSettingsTheme.kt#L9)（:68 使用）

- **描述**：`import com.tyranor.next.ui.common.WithoutPressIndication`，同时大量 ui 文件 import theme（如 GameScreen.kt:111-114、CoverScraperSettingsActivity.kt:54-59），theme 与 ui 互相依赖形成循环，导致 theme 包无法独立抽离/复用。

- **修复建议**：将 `WithoutPressIndication` 下沉到 theme/ 包（主题层公共修饰符），使依赖保持单向 ui→theme。

### m7. 破坏性迁移 + 未启用 exportSchema，未来 schema 演进存在静默清库回归窗口

- [ ] 待修复

- **类别**：架构 / 回归风险（注释已声明部分取舍）

- **位置**：[GameLibraryDatabase.kt:44-46](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/core/game/storage/GameLibraryDatabase.kt#L44-L46)

- **描述**：version=2（:24）且 `exportSchema=false`（:25）；:44 `fallbackToDestructiveMigrationOnDowngrade(dropAllTables=true)` + :46 `fallbackToDestructiveMigration(dropAllTables=true)`。v1→v2 破坏性重建是注释声明的已知取舍（prefs 镜像恢复），但存在两个残余风险：1) `GameLibraryRepository.kt:50` 的 prefs 镜像有 1500ms 防抖窗口（:313-323），窗口内写入无镜像可恢复；2) `exportSchema=false` 且无任何 Migration，未来 v3 起任何 schema 变更若忘记提供 Migration 将静默清空整库，engine\_detection\_cache（Artemis 启动记忆）也需重新学习。

- **修复建议**：下一个 schema 变更前启用 exportSchema 并配置 schema 目录、编写正式 Migration；将破坏性迁移收窄为仅降级场景（保留 OnDowngrade），升级路径强制显式迁移。

### m8. 存档管理页在主线程组合期及 refresh() 中执行目录递归遍历

- [ ] 待修复

- **类别**：代码健壮性（验证降级：SAF Binder 仅为兜底路径，主路径为字符串解析 + File 本地 IO）

- **位置**：[SaveManagementActivity.kt:113-122](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/save/SaveManagementActivity.kt#L113-L122)

- **描述**：:113-114 在 `remember` 初始化块中主线程调用 `manager.resolveSaveLocation(game)`（主路径 `safUriToPath` 字符串解析 + `File.isDirectory`；SAF Binder 查询仅路径解析失败后的兜底）与 `manager.listSaveFiles(game)`（:307-315 主线程递归遍历整个存档目录树）。:119-122 的 `refresh()` 在 `runSaveTask` 主线程段（`withContext(Dispatchers.IO)` 之外）同样调用。Artemis 游戏存档位置即游戏根目录，目录可达数万文件时首次打开与每次操作完成都会卡顿。

- **修复建议**：初始加载改为 `LaunchedEffect { withContext(Dispatchers.IO) { ... } }` 后回填 state；`refresh()` 内部整体包 `withContext(Dispatchers.IO)`，仅回写结果时回主线程。

### m9. runCatching 捕获 CancellationException，协程取消被误报为安装失败

- [ ] 待修复

- **类别**：代码健壮性

- **位置**：[KrkrOnlinePatchActivity.kt:223-233](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/patch/KrkrOnlinePatchActivity.kt#L223-L233)（:131 loadIndex 同）

- **描述**：:223 `runCatching { downloadAndInstall(...) }` 一并捕获 `CancellationException`：Activity 重建导致作用域取消时，取消异常被当 `onFailure` 处理，message 写入技术性异常文本，既吞掉取消信号破坏结构化并发，也向用户展示误导性错误。:131 `loadIndex` 的 runCatching 同（后果较轻）。

- **修复建议**：参照本项目 GameScreen.kt:962 既有模式：先 `catch (e: CancellationException) { throw e }` 再捕其他异常，或封装只捕获非取消异常的辅助函数。

### m10. 补丁下载无总字节数上限，极端输入可写满外部存储

- [ ] 待修复

- **类别**：代码健壮性

- **位置**：[KrkrOnlinePatchService.kt:115-127](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/core/patch/KrkrOnlinePatchService.kt#L115-L127)

- **描述**：`downloadToFile` 为 `input.copyTo(output)` 裸拷贝，无 contentLength 预检也无累计字节上限。索引内容为逐行正则解析（parseLine :86-101），若索引内容异常或 URL 指向超大资源，多选安装时可在无用户感知下写满磁盘。对比：`CoverImageCache.download` 有 20MB 上限（CoverSupport.kt:18/:48/:59），`GameSaveManager.extractZip` 有 1GB 上限（GameSaveManager.kt:488/:373），此处缺失。

- **修复建议**：复制循环累计已写字节数并设上限（如 200MB），超限抛 IOException 并清理半成品文件。

### m11. SaveActionCard 手写 Card+Row+KeyboardArrowRight 拼装条目

- [ ] 待修复

- **类别**：代码规范（验证降级：导出/导入/删除属动作而非「进入下一级」跳转，不完全落入 AppNavItem 强制范畴；且 AppNavItem 无左图标时会强加占位图标）

- **位置**：[SaveManagementActivity.kt:252-271](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/save/SaveManagementActivity.kt#L252-L271)

- **描述**：手写 Card(NavWhite+8dp)+Row(h16 v18.5)+Text(titleMedium)+Icon(KeyboardArrowRight, onSurfaceVariant)，与 AppNavItem 形态高度重复，且缺深色模式箭头自动染白适配，自行 import `Icons.AutoMirrored.Filled.KeyboardArrowRight`。

- **修复建议**：如需统一，先给 AppNavItem 增加 no-icon / danger 参数后改用之；否则保留但知晓与其规范组件存在双轨。

### m12. dialog.show() 后同步检查 continuation.isActive 为死代码

- [ ] 待修复

- **类别**：代码规范（无效代码）

- **位置**：[ArtemisPatchChoiceDialog.kt:97-100](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/game/ArtemisPatchChoiceDialog.kt#L97-L100)

- **描述**：`suspendCancellableCoroutine` 已注册 `invokeOnCancellation` dismiss（:96），:97 有前置 `if (continuation.isActive)` 检查；:98 `dialog.show()` 同步返回后 :99 再查 `isActive`——同一线程同步窗口内 isActive 不可能翻转，:99 分支不可达。

- **修复建议**：删除内层 if，仅保留 `dialog.show()`。

### m13. 硬编码 Color(0xFF17191C) 与 Color.kt 私有常量 DarkGrey 重复

- [ ] 待修复

- **类别**：代码规范（中性色收口）

- **位置**：[LiquidGlassNavigation.kt:87](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/common/LiquidGlassNavigation.kt#L87)

- **描述**：`Color(0xFF17191C)` 与 theme/Color.kt:18 `DarkGrey` 字面与语义完全相同，但因该常量为 private 而被再次硬编码，深色板调整时两处会漂移。违反「中性色必须统一收口到 Color.kt」。

- **修复建议**：将 Color.kt 的 DarkGrey 改为公开常量并在此引用。

### m14. 导航栏圆角 16dp 与「组件统一圆角 8dp」条款冲突（未登记豁免）

- [ ] 待修复

- **类别**：代码规范（跨页面一致性）

- **位置**：[LiquidGlassNavigation.kt:90](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/common/LiquidGlassNavigation.kt#L90)、:243

- **描述**：:89 注释一边引用规范一边声明「液态玻璃导航在 8dp 基础上加大 8dp」的有意偏离；:243 导航项胶囊同样 16dp 无说明。与 AGENT.md「组件统一圆角数值为 8dp」条文不一致，属未登记的例外，后续维护者无所适从。

- **修复建议**：二选一：导航栏回归 8dp 圆角，或在 AGENT.md 中为液态玻璃导航增加圆角豁免条款。

### m15. 页面硬编码中性色 Color(0xFF303338)

- [ ] 待修复

- **类别**：代码规范（验证降级：单点私有常量、带用途注释）

- **位置**：[HomeScreen.kt:472](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/home/HomeScreen.kt#L472)

- **描述**：`private val QuickLaunchFallbackBackground = Color(0xFF303338)`，该色值不在 theme/Color.kt（现有深色系为 0xFF222529 / 0xFF17191C）。违反「新增任何颜色先检查 Color.kt 是否已有现成常量；中性色必须统一收口」。

- **修复建议**：移入 theme/Color.kt 作为具名常量（如 QuickLaunchFallback），HomeScreen 引用。

### m16. EnumSliderRow 与 ContinuousSliderRow 近乎完全重复

- [ ] 待修复

- **类别**：代码规范（重复代码）

- **位置**：[SettingsScreen.kt:866-927](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/settings/SettingsScreen.kt#L866-L927)

- **描述**：两个函数（:866-897 与 :900-927）除 Slider 是否带 showKeyPoints/keyPoints/magnetThreshold/hapticEffect 四行磁吸+震动参数外逐行相同（同样 ArrowPreference 骨架、endActions 文本、valueRange、回调）。

- **修复建议**：合并为 `SliderRow(label, options, current, onSelect, discrete: Boolean)`，由 discrete 控制磁吸/震动参数。

### m17. BottomInsetSpacer 在两个文件逐字重复

- [ ] 待修复

- **类别**：代码规范（重复代码）

- **位置**：[SettingsScreen.kt:643-645](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/settings/SettingsScreen.kt#L643-L645)、[AppSettingsActivity.kt:319-321](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/settings/AppSettingsActivity.kt#L319-L321)

- **描述**：同名私有函数连 KDoc 注释「列表底部占位：避让系统导航栏」都一字不差，属跨文件复制。

- **修复建议**：移入 ui/common（如 common/Spacing.kt）供设置类页面共用。

### m18. 字体导入两处重复实现且行为漂移（一处不校验扩展名）

- [ ] 待修复

- **类别**：代码规范（重复代码 + 行为不一致）

- **位置**：[PerGameSettingsScreen.kt:502-509](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/settings/PerGameSettingsScreen.kt#L502-L509)、[SettingsScreen.kt:965-982](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/settings/SettingsScreen.kt#L965-L982)

- **描述**：两处均为「导入字体到 filesDir/fonts」的私有实现但行为漂移：SettingsScreen.importFont 先 contentResolver.query 取 DISPLAY\_NAME（更可靠）再做 .ttf/.ttc/.otf/.otc 白名单校验；PerGameSettingsScreen.copyFontToPrivate 仅用 `uri.lastPathSegment` 取名（provider 场景可能为 null 或含路径噪声）且完全不校验扩展名。

- **修复建议**：抽到 core 层（如 core/settings/FontImport.kt）或 ui/common 单一实现，统一扩展名校验与文件名解析。

### m19. 字体选择弹窗两处同构重复

- [ ] 待修复

- **类别**：代码规范（重复代码）

- **位置**：[PerGameSettingsScreen.kt:473-498](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/settings/PerGameSettingsScreen.kt#L473-L498)（OverrideFont）、[SettingsScreen.kt:929-963](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/settings/SettingsScreen.kt#L929-L963)（FontRow）

- **描述**：两组件均为「ArrowPreference + open 状态 + AppAlertDialog + Column{两个可点击 Row + 取消}」结构，其中「选择字体文件」项逐行相同；差异仅在第一项语义（跟随全局/清除覆盖 vs 使用内置字体/重置）与当前值展示位置。

- **修复建议**：合并为一个可选 override 参数的 FontPreference 组件（override == null 渲染全局版，否则渲染三态版）。

### m20. 7 个 Activity 重复手写 edge-to-edge 状态栏模板且已出现不一致

- [ ] 待修复

- **类别**：代码规范（重复代码 + 模板漂移）

- **位置**：[AppSettingsActivity.kt:65-73](file:///Users/weiss/opencode/rma/TyranorNext/app/src/main/java/com/tyranor/next/ui/settings/AppSettingsActivity.kt#L65-L73)（同模式：EngineSettingsMenuActivity:56-64、EngineSettingsActivity:24-32、CoverScraperSettingsActivity:72-76、SaveManagementActivity:69-73、KrkrOnlinePatchActivity:76-80、PerGameSettingsActivity:27）

- **描述**：「darkMode 判断 + enableEdgeToEdge 双分支」模板在除 MainActivity 外 7 个 Activity 重复。且前三者（AppSettings:70-73、EngineSettingsMenu:61-64、EngineSettings:29-32）在 enableEdgeToEdge 后又手写 `@Suppress("DEPRECATION") window.statusBarColor/navigationBarColor = TRANSPARENT`，而 MainActivity:44 与 PerGameSettingsActivity:31 注释明确「透明沉浸由 enableEdgeToEdge 处理，无需再设置已弃用的 window\.statusBarColor」——前冗余后简洁，模板漂移。

- **修复建议**：抽取 Activity 扩展函数（如 `ComponentActivity.setupTransparentSystemBars()`）统一模板；删除 enableEdgeToEdge 之后冗余的 window\.statusBarColor 赋值。

***

## 审查通过项（未发现问题）

- **依赖方向总体成立**：core/ 无任何 `import com.tyranor.next.ui.*`；ui/theme/根包无任何 `com.core / com.akira / com.yuri / org.tvp / org.libsdl / org.cocos2dx / bridge` 引擎包 import（21 处引擎包 import 全部位于 core/ 内，属规范允许）。

- **EngineType 分发完备**：11 个枚举值在 `EngineLauncher.buildIntent` 的 `when` 全覆盖（编译期强制）；各引擎 Intent extras 契约与 engine 侧消费点一一对应（主题色注入 → EngineThemeColors/KrDialogStyle、Artemis 版本回退 → ArtemisLauncherBaseActivity、krkr prefs → KirikiroidLauncherBaseActivity，`KR_RENDER_PREF_KEYS`(10) 与 `KR_FIELDS`(10) 数量匹配）。

- **外置引擎注册表无冲突**：RENPY 双模块由 `resolveModule` 按版本解析。

- **PrefsRenameMigration 无破坏性**：标记键 + 「目标缺失才拷贝」幂等合并，旧文件保留可回滚，全进程执行满足声明。

- **Room 与进程边界**：`GameLibraryRepository.init` 有 `isMainProcess` 守卫，引擎子进程不触库；prefs 镜像 + `syncFromPrefsLocked` 恢复链路真实存在。

- **主题规范核心合规**：两个主题函数均 `primaryColor: Color? = null` + 函数体内读取 + `@NonSkippableComposable` + `ensureLoaded`；无 `colorScheme.surface*` 容器引用。

- **组件规范合规**：无 Material TopAppBar/Scaffold/TextField/Switch 违规；顶部栏图标全用 TopBarIcon；搜索/输入框全走 AppSearchField（AppSearchField 内部 Miuix SearchBar/InputField 属组件固定模板，合规）。

- **健壮性防护到位**：`GameSaveManager` 原子导入+备份回滚、`ArtemisPfsUnpacker` 路径穿越/大小限制、`EnginePluginBootstrap` zip-slip 防护、`SecureAuthStore` Keystore 兜底、封面 20MB / 存档 zip 1GB 上限、Room 事务边界、LIKE 转义与 ESCAPE、SQL 分片 500 上限、CancellationToken 取消点覆盖。

## 修复优先级建议

1. **P0（数据正确性）**：M1（vndbId 丢失）、M4（补丁并发损坏）、m4（令牌丢失）
2. **P1（明文规范违反，改动小）**：M5、M6、M7、m12、m13、m15
3. **P1（架构）**：M2、M3、m6
4. **P2（需全局改造）**：M9、M10（顶部栏统一，涉及全部页面）、M11、m11
5. **P3（低风险清理）**：其余 minor

